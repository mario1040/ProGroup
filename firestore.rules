rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read/write since authentication is handled via custom usernames/passwords without Firebase Auth
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
